# Sistem Login Aman — dependensi (Python / Flask)
# Catatan: QR code dihasilkan sebagai SVG murni-Python (qrcode[svg]),
# sehingga TIDAK memerlukan pillow/zlib — kompatibel dengan Python 3.14.
Flask>=3.0,<4.0
Flask-Session>=0.8
bcrypt>=4.2
pyotp>=2.9
qrcode>=7.4
